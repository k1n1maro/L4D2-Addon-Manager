# 🚀 Инструкция по настройке автопостинга в Steam

## 📁 1. Структура файлов

Разместите файлы в корне вашего GitHub репозитория:

```
L4D2-Addon-Manager/
├── .github/
│   └── workflows/
│       └── steam_promote.yml      # GitHub Actions workflow
├── steam_autopost.py              # Основной скрипт
├── posted_versions.txt            # История опубликованных версий
└── requirements.txt               # Зависимости Python
```

## 🔐 2. Настройка GitHub Secrets

Перейдите в настройки репозитория:
```
GitHub → Ваш репозиторий → Settings → Secrets and variables → Actions → New repository secret
```

Создайте два секрета:

### STEAM_LOGIN
- Name: `STEAM_LOGIN`
- Secret: ваш логин Steam (username)

### STEAM_PASSWORD
- Name: `STEAM_PASSWORD`
- Secret: ваш пароль Steam

⚠️ **ВАЖНО**: Эти данные будут храниться зашифрованными и недоступны никому, кроме GitHub Actions.

## 🧪 3. Локальное тестирование

### Установка зависимостей:
```bash
pip install -r requirements.txt
```

### Запуск теста:
```bash
# Windows
set STEAM_LOGIN=your_login
set STEAM_PASSWORD=your_password
set RELEASE_VERSION=v1.2.0
python steam_autopost.py

# Linux/Mac
export STEAM_LOGIN=your_login
export STEAM_PASSWORD=your_password
export RELEASE_VERSION=v1.2.0
python steam_autopost.py
```

### Что произойдёт:
1. Откроется Chrome
2. Скрипт залогинится в Steam
3. **Если требуется Steam Guard** — введите код вручную в браузере
4. Скрипт создаст пост в L4D2 Discussions
5. Версия добавится в `posted_versions.txt`

## 📦 4. Создание первого релиза

### Через GitHub Web:
1. Перейдите: `Releases → Draft a new release`
2. Заполните:
   - **Tag**: `v1.2.0` (или любая версия)
   - **Title**: `Version 1.2.0`
   - **Description**: описание изменений
3. Нажмите **Publish release**

### Через командную строку:
```bash
git tag v1.2.0
git push origin v1.2.0
gh release create v1.2.0 --title "Version 1.2.0" --notes "Release notes here"
```

## ⚙️ 5. Как это работает

1. **Триггер**: При публикации релиза срабатывает `steam_promote.yml`
2. **Проверка**: Скрипт проверяет `posted_versions.txt` — не был ли релиз уже опубликован
3. **Авторизация**: Логин в Steam (Steam Guard нужно подтвердить вручную при первом запуске)
4. **Публикация**: Создаётся пост в формате:
   ```
   Title: L4D2 Addon Manager v1.2.0 — simple addon control tool
   Body: [описание с ссылкой на релиз]
   ```
5. **Запись**: Версия добавляется в `posted_versions.txt` и коммитится обратно в репозиторий

## 🔧 6. Решение проблем

### Steam Guard блокирует вход
**Решение**: При первом запуске в GitHub Actions нужно:
1. Временно отключить headless режим (закомментировать строку в `steam_autopost.py`)
2. Запустить локально и подтвердить Steam Guard
3. После этого Steam запомнит устройство

### Пост не создаётся
**Проверьте**:
- Правильность логина/пароля в Secrets
- Логи GitHub Actions: `Actions → последний workflow → post-to-steam`
- Селекторы могут измениться — обновите их в `steam_autopost.py`

### Дублирование постов
**Защита**: Скрипт автоматически проверяет `posted_versions.txt` перед публикацией.

## 📝 7. Кастомизация

### Изменить текст поста:
Отредактируйте переменную `body` в функции `create_post()` файла `steam_autopost.py`

### Изменить раздел обсуждений:
Замените URL в функции `navigate_to_discussions()`:
```python
self.driver.get("https://steamcommunity.com/app/550/discussions/")
```

### Добавить скриншоты:
Расширьте функцию `create_post()` для загрузки изображений через Selenium.

## ✅ Готово!

Теперь каждый новый релиз будет автоматически публиковаться в Steam Community.

---

**Вопросы?** Создайте Issue в репозитории.
